**东软电动汽车充电桩应用管理平台**
**成员 2 任务说明书**
**充电用户端（Linux + Qt）**
**用途：拿到文档即可理解负责范围，并按完整功能增量组织提交。**

# 一、任务界定

| **你的角色： ****用户 Qt 客户端负责人。你负责把“车主从登录、找站、找桩，到充电、结算、维护个人信息”的完整用户旅程做成一个可用的 Qt 桌面应用。你的重点不是堆页面，而是把页面状态与服务端真实业务状态正确绑定。** |
|---|
 
## 1. 你负责的范围

**• 手机号免密登录/自动注册****输入 11 位手机号；老用户直接登录，新手机号由服务端自动建用户。登录后得到昵称、头像、余额等信息。**
**• 附近充电站查询****支持区域下拉/地址输入模拟定位；调用腾讯地图 Web API 做地址转经纬度；展示按距离排序的站点卡片、价格、总桩/空闲桩和距离。**
**• 站点详情与一键导航****查看站内所有桩的编号、类型、状态、功率；从当前位置到目标站点使用腾讯地图路线规划，并支持驾车/步行等方式。**
**• 用户信息维护****展示和修改昵称、头像；模拟充值并实时刷新余额。**
**• 完整充电流程****进入前检查未完成订单；选择空闲桩；预约/开始；展示充电状态；结束后计费并结算；处理余额不足和重复操作等情况。**

## 2. 这些任务到底意味着什么

### 1）你不是“把页面画出来”就完成了

**项目中的用户端是一个有业务状态的客户端。比如充电按钮能否点击，不能仅看界面上的“空闲”字样，而要在提交预约时再接受服务器最终判断；网络断开时，也不能假装操作成功。你需要把 UI 状态、网络请求状态和服务器业务状态区分开。**
**建议把网络通信封装成独立 NetworkClient，把服务端返回映射成 DTO/Model，再由页面消费。这样后面接口字段变化时不需要在所有按钮槽函数里到处改 JSON。**

### 2）“附近充电站”包含地图能力和业务数据两部分

**腾讯地图 API 负责地址→经纬度以及路线规划；真正的站点价格、空闲桩数、桩详情来自项目服务端。不要把地图 API 当成充电站数据源。用户地址解析成功后，拿经纬度请求服务端附近站点，再渲染卡片。**

### 3）充电流程要按状态机做，而不是一串按钮

**说明书明确要求进入充电页先检查未完成订单，并覆盖“预约—充电—计费—结算”。因此页面应该围绕订单状态切换：未预约、已预约、充电中、待结算、已完成。服务器是状态真相来源，客户端负责正确展示和引导。**

## 3. 你的责任边界

**• 你不负责：****直接修改 SQLite、定义最终业务规则、管理员页面、大屏和模型训练。用户端所有持久化业务应通过成员 1 的服务端。**
**• 你需要主动找成员 1：****任何接口字段、状态枚举、错误码不清楚；充电流程出现“页面认为成功但服务端拒绝”的情况。**
**• 你需要和成员 3/4 协作：****跨端验证用户操作是否在管理员端/大屏反映；若接入低拥堵站点推荐，则与成员 4 对齐预测结果含义。**

## 4. 你最终要交出的东西

**• ****可运行的 Linux + Qt 用户客户端；统一网络层与业务 DTO；登录/资料/充值；地址定位与附近站点；站点详情与导航；完整充电结算状态机；主要错误/空状态/断网交互。**

# 二、开发计划与 Commit 节点拆解

**下面的节点不是“按天排期”，而是一条建议的开发依赖顺序。原则是：只有当一个功能或基础能力已经形成清晰、可运行、可回滚的增量时才提交；不要为了凑提交次数把一个尚未闭环的功能切成大量碎片 commit。**

| **提交原则： ****一个 commit 应能用一句话说明“它新增/修复了什么”，并且回退这个 commit 时影响范围明确。若某个节点工作量较大，可以在不破坏功能闭环的前提下拆成 2~3 个 commit，但不要把“改一个按钮颜色、改一个变量名”当成功能提交。** |
|---|

## Commit 0：搭好用户端工程骨架、页面导航和网络层

| **这个 commit 的目的： ****让后续每个页面都建立在统一的导航、会话和网络能力上，避免每个页面自己 new socket、自己解析 JSON。** |
|---|

**建议 commit：****feat(user-client): bootstrap app shell navigation and network client**
**1. 建立页面/模块结构：****搭出登录页、主页/站点列表、站点详情、充电页、个人中心等容器与基本导航。此时界面可以简洁，但页面切换、返回关系和登录态入口要清楚。**** 关键判断：****可以从登录进入主界面并在主要页面间切换，不出现重复窗口/失控返回。**
**2. 封装 NetworkClient：****按成员 1 的 framing 和消息外壳实现连接、发送、接收、requestId 匹配、JSON 解析和错误信号。业务页面只调用 sendRequest(action, data) 一类接口，不直接碰底层 socket 缓冲。**** 关键判断：****能对 PING 发起多个并发/连续请求并正确匹配响应。**
**3. 建立 Session/用户上下文：****集中保存当前 userId、手机号、昵称、余额、头像等登录态数据，并提供刷新信号。不要让每个页面各保存一份余额副本。**** 关键判断：****修改 Session 后相关页面可以统一刷新。**

| **协作点： ****本节点首先找成员 1 获取消息边界、公共请求/响应结构和错误码。若协议尚未完成，用约定好的 mock 示例开发，但 NetworkClient 的边界要和服务端一致。** |
|---|

**验收边界**
**• ****工程结构能承载后续功能；主要页面可导航。**
**• ****网络层独立于页面，并能稳定发送/接收协议消息。**
**• ****登录态有唯一来源而不是散落在 UI 控件里。**

## Commit 1：实现手机号登录与自动注册体验

| **这个 commit 的目的： ****跑通第一个真实业务闭环：输入手机号→请求服务端→得到/创建用户→进入主界面并建立 Session。** |
|---|

**建议 commit：****feat(user-client): add phone login and auto-registration flow**
**1. 做好输入校验：****手机号输入框限制/验证 11 位数字；空值、格式错误在本地直接提示，不要把明显无效请求都发给服务器。登录请求发送期间禁用重复点击并显示进行中状态。**** 关键判断：****连续点击不会发出多次重复登录。**
**2. 对接登录接口：****解析服务端返回的 userId、nickname、balance、status、avatar 等字段。新用户和老用户对客户端不必分成两个页面，但可以提示首次创建成功。**** 关键判断：****新手机号第一次进入后，第二次登录不再被当作新用户。**
**3. 处理冻结/网络/服务错误：****根据服务端 code 显示可理解的提示，网络断开和业务拒绝要区分。不要只弹“登录失败”。**** 关键判断：****至少能区分格式问题、服务器不可达、账号状态问题。**

| **协作点： ****如果成员 1 对“冻结用户是否允许登录”没有固定规则，在此节点直接确定并写进接口契约；不要在客户端私自猜。** |
|---|

**验收边界**
**• ****合法手机号能成功进入主界面并正确显示用户信息。**
**• ****首次手机号能自动注册；重复登录不重复建用户。**
**• ****失败分支不会留下半登录 Session。**

## Commit 2：实现个人中心、昵称/头像修改和充值

| **这个 commit 的目的： ****完成说明书中的用户信息维护，并验证用户数据修改可以真实持久化。** |
|---|

**建议 commit：****feat(user-client): add profile editing avatar and recharge**
**1. 完成个人中心展示：****展示头像、昵称、手机号（如需要）和钱包余额；所有值从 Session/服务端加载，不写死。进入页面时可刷新一次，避免长期显示旧数据。**** 关键判断：****退出重登后展示与数据库一致。**
**2. 实现昵称修改：****输入合法性检查后调用服务端更新接口；成功后更新 Session，失败则保留旧值。不要“先改 UI 再假设服务器会成功”。**** 关键判断：****修改后其他页面需要显示昵称的地方同步更新。**
**3. 实现头像选择与传输：****使用 QFileDialog 选本地图片，做格式/尺寸检查和必要缩放。头像具体存储/传输方式由成员 1 与你共同冻结；如果用 Base64，控制图片大小，避免把数 MB 原图塞进 JSON。**** 关键判断：****重启应用/重新登录仍能看到头像，而不是只在本次 UI 中存在。**
**4. 实现充值交互：****输入金额后请求服务器执行模拟充值；成功返回新余额后再刷新 Session。对零/负数/极端金额做校验，防止重复点击。**** 关键判断：****服务端余额和 UI 余额完全一致，重复响应不会重复加钱。**

| **协作点： ****头像方案必须和成员 1 一起定；充值后的用户余额可让成员 3 在用户管理页核对，作为第一次跨端一致性验证。** |
|---|

**验收边界**
**• ****昵称、头像、充值均通过服务端持久化。**
**• ****成功与失败状态明确；不会出现 UI 显示已改但服务器没改。**
**• ****管理员端刷新后能看到一致的昵称/余额/状态数据。**

## Commit 3：接入地址定位与腾讯地图地理编码

| **这个 commit 的目的： ****把说明书中的“下拉区域/手动地址→经纬度”独立跑通，为附近站点查询和导航提供真实坐标。** |
|---|

**建议 commit：****feat(user-client): add address geocoding with Tencent Maps API**
**1. 设计定位输入：****支持区域选择或手动地址输入，整理成一个最终地址字符串。可以保留“当前位置”为 Session 中的一组坐标/地址，用于后续距离和导航。**** 关键判断：****用户能看见当前使用的是哪一个位置，而不是坐标藏在后台。**
**2. 用 QNetworkAccessManager 调地理编码 API：****构造请求、管理 key、解析腾讯地图返回的经纬度与错误状态。API key 不要散落在多个源码文件，可放配置。**** 关键判断：****对一个明确地址能拿到合理经纬度，并可重复调用。**
**3. 处理外部 API 不可用：****网络失败、key 无效、地址解析不到都要有明确 UI 状态。为了演示稳定，可以保留“选一个预置区域/坐标”的降级方式，但要标注是软件模拟定位。**** 关键判断：****地图 API 失败不会让整个主界面卡死。**

| **协作点： ****这一节点主要不依赖成员 1，但需要确认“附近站点接口是由服务端计算距离，还是客户端拿全部站点后自己计算”。二者选一个并冻结。** |
|---|

**验收边界**
**• ****地址可稳定转换成经纬度并写入当前定位状态。**
**• ****失败有可理解提示和可恢复路径。**
**• ****定位结果能直接作为下一节点查询附近站点的输入。**

## Commit 4：实现附近充电站列表与距离排序展示

| **这个 commit 的目的： ****完成用户“找桩”的主入口：把定位坐标与服务端站点业务数据结合起来。** |
|---|

**建议 commit：****feat(user-client): add nearby station list and station cards**
**1. 请求附近站点：****把当前经纬度传给服务端（若服务端负责距离排序），拿到站名、价格、总桩/空闲桩、距离、stationId 等。对空列表和超时分别处理。**** 关键判断：****改变定位后列表会按新位置刷新，而不是继续显示旧数据。**
**2. 实现可复用站点卡片：****每张卡清楚显示说明书要求的关键字段，并保留 stationId 作为点击详情的业务 id。不要用列表索引代替 stationId。**** 关键判断：****列表刷新/排序后点击任何卡仍能进入正确站点。**
**3. 处理刷新与加载状态：****请求中显示 loading，返回后替换数据；避免用户快速重复定位造成旧响应覆盖新响应，可用 requestId 或“当前查询版本”判断。**** 关键判断：****快速连续查询时最终页面对应最后一次定位。**

| **协作点： ****字段不足就找成员 1 补接口，不要自行写死“空闲桩数”。距离算法若放客户端，也要与成员 1 保持唯一口径。** |
|---|

**验收边界**
**• ****站点卡片字段完整且来源真实。**
**• ****列表顺序与距离规则一致；空数据、超时、重复刷新可控。**
**• ****点击卡片能携带正确 stationId 进入详情。**

## Commit 5：实现站点详情、桩列表和一键导航

| **这个 commit 的目的： ****让用户从“看到站点”继续到“选择具体桩，并能导航过去”。** |
|---|

**建议 commit：****feat(user-client): add station detail charger list and route navigation**
**1. 加载站内充电桩详情：****按 stationId 请求桩列表，显示编号、类型、状态、功率；用明显样式区分空闲/在用/故障。只有满足服务器规则的空闲桩才允许进入充电选择。**** 关键判断：****站点没有桩或接口失败时页面仍能正常展示空状态。**
**2. 实现桩选择：****点击空闲桩后记录 chargerId，并进入充电流程前置检查。不要在这个页面直接把桩状态本地改为“已占用”。真正预约成功由服务端确认。**** 关键判断：****被其他客户端抢先占用时，服务端拒绝后能提示并刷新列表。**
**3. 实现腾讯地图路线规划：****用 QWebEngineView 加载腾讯地图路线页面/URL，起点使用当前定位，终点使用站点坐标；为驾车/步行提供选择。外部页面加载失败要可返回主流程。**** 关键判断：****选择不同站点，终点坐标对应正确。**

| **协作点： ****桩状态含义必须使用成员 1 的枚举；导航仅依赖地图 API，不要让成员 1 负责网页导航逻辑。** |
|---|

**验收边界**
**• ****站点详情字段完整；只有合法桩可进入下一步。**
**• ****并发占用导致失败时能刷新真实状态。**
**• ****路线规划可从当前定位到目标站点，并支持要求的出行方式。**

## Commit 6：实现未完成订单检查、预约和开始充电

| **这个 commit 的目的： ****开始实现核心订单状态机，确保用户不会在有未完成订单时再次开启新充电。** |
|---|

**建议 commit：****feat(user-client): add charging precheck reservation and start flow**
**1. 进入充电前执行未完成订单检查：****无论用户从哪里进入充电流程，都先请求当前用户的 active order。如果存在，按说明书弹提示并跳转对应结算/继续页面；不存在才允许选择新桩。**** 关键判断：****通过返回键/重复入口也无法绕过检查。**
**2. 发起预约并处理冲突：****向服务端提交 chargerId，只有收到成功后才把 UI 切成“已预约”。若桩已被抢占，刷新该站桩状态并让用户重新选择。**** 关键判断：****客户端本地不会制造“假预约成功”。**
**3. 开始充电：****调用 start 接口，成功后进入充电中页面，并以服务端订单信息作为开始时间/订单号来源。按钮在请求处理中禁用，防止重复 start。**** 关键判断：****重复点击开始不会产生多个订单或非法状态。**
**4. 做页面状态机：****把 RESERVED、CHARGING、WAIT_SETTLEMENT 等状态映射到可见 UI 和允许动作。推荐集中写一个 renderOrderState(order) 而不是多个槽函数随意 show/hide。**** 关键判断：****给定任意合法订单状态，页面显示和按钮可用性是确定的。**

| **协作点： ****这个节点必须和成员 1 的订单状态机同步。如果你发现页面需要一个状态但接口没有，不要自己新增字符串；一起调整公共枚举。** |
|---|

**验收边界**
**• ****有未完成订单时被强制引导；无订单时可成功预约/开始。**
**• ****桩冲突、重复操作均由服务端结果驱动。**
**• ****UI 状态与订单状态一一对应。**

## Commit 7：实现充电中展示、停止、计费与结算

| **这个 commit 的目的： ****闭合“充电—计费—钱包扣款—订单完成”主链路。** |
|---|

**建议 commit：****feat(user-client): complete charging stop billing and settlement flow**
**1. 展示充电中信息：****显示订单号、桩信息、开始时间和服务端可提供的实时/模拟时长、充电量、预估金额。若没有实时推送，可以定时请求订单状态；不要在客户端自行决定最终费用。**** 关键判断：****刷新页面/重新进入后仍能从服务端恢复当前订单。**
**2. 停止充电：****发送 stop 请求，成功后进入待结算，展示服务端计算的最终时长、充电量、金额。网络失败时不要直接改状态，允许重新获取订单。**** 关键判断：****stop 重试不会产生重复费用或把订单跳过结算。**
**3. 执行结算：****结算按钮调用服务器扣款；余额不足时显示金额差额/提示充值并保留待结算订单。成功后刷新 Session 余额，显示完成页。**** 关键判断：****成功结算后再次获取 active order 应为空，余额等于服务器返回值。**
**4. 处理恢复场景：****应用崩溃/重新登录后，如果服务端仍有 CHARGING 或 WAIT_SETTLEMENT 订单，必须能恢复到正确页面，而不是重新选桩。**** 关键判断：****重启客户端不会丢失业务进度。**

| **协作点： ****结算后请成员 3 核对营收和桩状态，成员 4 后续核对订单是否进入历史数据。这是主业务链第一次真正跨三个模块。** |
|---|

**验收边界**
**• ****从空闲桩到结算完成全链路可跑通。**
**• ****余额不足、网络中断、重开应用均不会让订单消失或产生重复扣款。**
**• ****结算后用户余额、桩状态、管理员营收保持一致。**

## Commit 8：补齐错误态、空态和网络恢复

| **这个 commit 的目的： ****让客户端不仅 happy path 能演示，常见异常也不会把页面留在错误状态。** |
|---|

**建议 commit：****fix(user-client): harden empty error and reconnect states**
**1. 统一业务错误展示：****按成员 1 错误码把“账号冻结、桩不可用、余额不足、订单状态冲突、参数错误”等转换为可理解文案，同时保留通用 fallback。**** 关键判断：****页面不再到处 hardcode 不同版本的错误字符串。**
**2. 处理断线重连：****Socket 断开时清理 pending 请求、提示离线；恢复后可重新连接并按需刷新当前页面/订单状态。不要自动把失败请求都盲目重发，尤其是充值/结算。**** 关键判断：****断网再恢复后能继续查询，敏感写操作不会重复执行。**
**3. 补空列表与 loading：****站点为空、桩为空、头像为空、订单为空都应有明确占位，不让用户误以为程序卡死。**** 关键判断：****每个主要页面至少有 loading / success / empty-or-error 三种可见状态。**

| **协作点： ****把你遇到的服务端错误码缺口整理给成员 1，一次补齐；不要在 UI 层用 message 文本猜业务类型。** |
|---|

**验收边界**
**• ****断网、空数据、业务拒绝时应用可恢复。**
**• ****写操作不会因自动重试发生重复副作用。**
**• ****主要页面状态完整，演示不依赖“永远成功”。**

## Commit 9：完成跨端联调与最终用户旅程收口

| **这个 commit 的目的： ****验证“用户做了什么，全系统就真实发生了什么”，并把最终演示路径整理成稳定版本。** |
|---|

**建议 commit：****chore(user-client): finalize end-to-end user journey and integration**
**1. 跑完整用户旅程：****新手机号登录→定位→找站→看桩→导航→预约→开始→停止→结算→个人中心看余额，全部使用真实服务端而非 mock。**** 关键判断：****重置数据后可重复走通。**
**2. 做跨端一致性核对：****充值后让成员 3 看余额；预约/充电后看管理员桩状态；结算后核对营收；若成员 4 已接预测，确认推荐字段/拥堵信息不会和真实空闲桩状态混淆。**** 关键判断：****任何一个数都能追溯到服务端/数据库来源。**
**3. 清理 mock 和临时代码：****删除硬编码站点、假余额、临时 JSON；保留必要的开发 mock 时必须有开关且默认关闭。**** 关键判断：****正常运行路径完全依赖真实接口。**

| **协作点： ****最终验收至少与成员 1、3 一起跑一次主流程；涉及预测推荐再找成员 4 验证语义。** |
|---|

**验收边界**
**• ****说明书要求的用户端功能全部可在真实数据上演示。**
**• ****跨端变化一致；不需要手工改数据库配合演示。**
**• ****客户端重启/异常恢复后仍保持正确业务状态。**

| **完成标准： ****当最后一个 commit 验收通过时，你的模块应当可以在不依赖临时手工改库、假数据或口头补充规则的情况下，与其他成员模块一起跑通项目说明书要求的主流程。** |
|---|

