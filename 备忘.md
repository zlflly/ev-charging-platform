# 服务器端开发备忘

## 项目角色
我是成员1：服务器端与数据库负责人，同时承担技术统筹角色。

## 核心职责
- PC服务器端（Linux + Qt）：Socket服务、请求分发、并发处理、业务逻辑
- 数据库端：SQLite数据模型设计、数据访问封装、种子数据
- 用户侧业务接口：登录注册、资料余额、站点桩查询、充电全流程
- 管理员侧业务接口：管理员登录、用户管理、站点桩管理、营收统计
- 统计与数据出口：为管理端、大屏、机器学习提供一致数据

## 已拍板决策（2026-09-04）
1. **代码位置**：服务端放仓库顶层 `server/` 目录，与 team-reference 中 `user-client/` 的层级对齐。
2. **技术栈**：Qt Core —— `QTcpServer` 收发、`QJsonDocument` 解析、`QThreadPool` 并发、`QtSql` 访问 SQLite。
3. **协议冻结**：客户端 `docs/协议对齐备忘.md` 第 5 节的 8 个待确认项**全部采纳客户端现状并冻结**。
   即：距离由服务端计算并排序；订单类操作靠**连接绑定 userId** 识别（无 token）；
   采纳 `1003 未登录`；桩状态 0/1/2/3、桩类型 0/1；订单状态四态字符串枚举。
   → 成员 2 已写好的客户端代码无需任何改动。

## 架构参考来源
- team-reference/user-client：用户端已实现的协议、数据模型、网络层
- team-reference/docs/协议对齐备忘.md：必须严格对齐的协议约定
- team-reference/DESIGN.md：整体设计语言（服务端不直接实现UI，但需理解业务语义）

## 协议约定（必须遵守）

### 传输层
- **长连接TCP**，一用户一连接
- **帧格式**：4字节大端长度头 + UTF-8 JSON payload（长度不含头部自身）
- **payload上限**：16MB（防御性）

### 消息外壳
**请求**：
```json
{"action": "user.login", "requestId": "req-1", "data": {...}}
```

**响应**：
```json
{"requestId": "req-1", "code": 0, "message": "ok", "data": {...}}
```
- `requestId`由客户端生成，服务端**必须原样回传**
- `code` = 0表示成功

### 错误码（已冻结）
| code | 含义 | 场景 |
|------|------|------|
| 0 | 成功 | - |
| 1001 | 参数格式错误 | 手机号非法、字段缺失 |
| 1002 | 账号被冻结 | 用户被管理员冻结 |
| 1003 | 未登录 | 订单类操作未先登录 |
| 2001 | 桩不可用/被占用 | 预约/开始充电 |
| 2002 | 余额不足 | 结算 |
| 2003 | 订单状态冲突 | 重复预约、状态非法迁移 |
| 5000 | 服务端内部错误 | fallback |

### 枚举定义（已冻结）

**充电桩状态 (ChargerStatus)**：
- 0 = 空闲（可预约）
- 1 = 充电中（在用）
- 2 = 故障
- 3 = 离线

**充电桩类型 (ChargerType)**：
- 0 = 快充
- 1 = 慢充

**订单状态 (OrderStatus，字符串枚举)**：
- `RESERVED` → `CHARGING` → `WAIT_SETTLEMENT` → `FINISHED`

### 用户身份识别
- 采用**连接绑定用户**方式（无token）
- `user.login`成功后将userId绑定到该TCP连接
- 后续订单操作按连接归属识别用户
- 未登录连接执行订单类操作返回1003

### Action列表（客户端已实现）
- `PING`：心跳/健康检查
- `user.login`：手机号登录/自动注册
- `user.profile.update`：修改昵称/头像
- `user.recharge`：充值
- `station.nearby`：附近站点（**服务端计算距离并排序**）
- `station.detail`：站点详情+桩列表
- `order.active`：查询未完成订单
- `order.reserve`：预约充电桩
- `order.start`：开始充电
- `order.status`：查询充电中实时数据
- `order.stop`：停止充电（**幂等**）
- `order.settle`：结算扣款
- `order.history`：订单历史

## 数据库设计要点
- 用户表：phone唯一，包含nickname/avatar/balance/status
- 管理员表：admin账号密码
- 充电站表：name/address/latitude/longitude/pricePerKwh
- 充电桩表：属于station，包含code/type/status/powerKw
- 订单表：关联user和charger，包含完整时间/电量/金额字段
- **状态字段与C++枚举、接口返回值必须一一对应**

## 关键业务规则
1. **手机号自动注册**：首次登录自动创建用户，昵称默认"用户+手机号后4位"
2. **一人一单**：同一用户同时最多一个未完成订单
3. **桩预约原子性**：预约时检查桩空闲并在同一事务中创建订单、更新桩状态
4. **费用服务端计算**：金额 = 充电量 × 站点价格，客户端不能指定金额
5. **结算事务性**：扣款、订单完成、桩恢复空闲必须在同一事务
6. **幂等性**：order.stop可重复调用，不重复计费

## 开发原则（用户长期要求，每次开发都适用）
- **commit 粒度要比任务说明书更细**：说明书的 Commit N 只是依赖顺序，实际按 N.1 / N.2 … 拆成小提交
- **每个大任务（说明书的一个 Commit）做完后必须停下来提醒用户检查**，不要连着往下做
- **架构严格参考 team-reference**：目录分层、命名风格、代码组织都向 user-client 看齐
- **协议严格对齐 team-reference/docs/协议对齐备忘.md**，不擅自改动已被客户端使用的字段
- 本备忘（`备忘.md`）是长期上下文，每次开发前先读，有新决策就写回来
- 网络层、协议层、业务层、数据层清晰分离
- 处理TCP粘包/半包、连接异常、并发冲突
- 所有写操作使用事务保证一致性
- 可重复初始化的种子数据
- 结构化日志记录requestId、action、关键实体id

## 开发工作流（2026-09-04 更新）
- **不在Windows本地编译Qt**：用户会将代码放到虚拟机Linux环境中编译和测试
- **不运行测试**：可以编写测试程序，但不在本地执行（用户会自行在Linux虚拟机测试）
- **逐步完成任务并自主commit**：每完成一个小功能点立即git commit，commit消息要具体描述完成内容
- **每次commit后更新备忘**：记录当前进度到本文档的"当前状态"部分

## 协议对齐文档位置

**`docs/协议冻结说明.md`** 是全队协议对齐的唯一权威文档，已完成：
- 传输层、消息外壳、错误码（已冻结）
- 枚举定义：充电桩状态/类型、订单状态、用户状态（已冻结）
- 用户端 13 个 action 完整定义（已冻结）
- 管理员端 action 列表框架（Commit 6 补齐）
- 机器学习数据接口约定（Commit 7 补齐）
- 数据库表设计（已冻结）
- 与成员 2/3/4 的对齐结论

**请将此文档同步给队友**，任何协议变更必须先更新此文档并通知受影响成员。

## 待办事项

### Commit 0 拆解方案（已与用户对过，正在进行）
说明书 Commit 0 = `feat(server): bootstrap socket server and shared protocol envelope`，
拆成 7 个小 commit，分两段、两个检查点：

**第一段（协议先行，做完停下让用户检查协议）**
- 0.1 建立 `server/` 工程骨架：CMakeLists + 目录分层（`src/config`、`src/net`、
  `src/protocol`、`src/service`、`src/repository`、`tests`、`tools`），main 可编译可启动
- 0.2 启动配置：监听地址、端口、数据库路径从命令行参数/配置文件读取，不写死在源码
- 0.3 协议定义：服务端版 `Protocol.h`，镜像客户端已冻结的外壳、错误码、枚举、action 常量

**第二段（网络骨架，做完用户应能手动连上发 PING）**
- 0.4 framing 编解码器 + 单元测试：4 字节大端长度头，覆盖半包/粘包/超长包/非法长度
- 0.5 TCP 监听与连接生命周期：QTcpServer 接受连接，每连接独立缓冲区，断连释放资源
- 0.6 请求路由 + PING/HEALTH：统一 router 分发，未知 action / 非法 JSON 返回稳定错误码不崩溃
- 0.7 冒烟测试客户端（`tools/`）：连发 100 个请求验证不粘包不截断

**Commit 0 完成后要交付给成员 2、3 的东西**：消息外壳 + framing 规则 + 错误码表。

### 当前状态（2026-09-04 更新）

**✅ Commit 0 已完成**：网络骨架、framing、PING（详见前）

**✅ Commit 1 已完成**：数据库模型与Repository层（详见前）

**✅ Commit 3 已完成**：
- ✅ **SessionManager**：连接→userId/adminId绑定，断连自动清除会话
- ✅ **UserService**：用户业务逻辑封装
  - `handleLogin`: 手机号校验（11位1开头）、自动注册、冻结检查、连接绑定
  - `handleProfileUpdate`: 昵称更新（≤20字符）、头像data-URL上传（≤256KB）
  - `handleRecharge`: 充值金额校验（>0，≤2位小数）、余额事务更新
- ✅ **RequestDispatcher**：action路由 + 登录拦截
  - 注册3个用户端action：user.login / user.profile.update / user.recharge
  - `requiresAuth()`: 区分公共接口（PING/user.login）与需登录接口
  - 未登录访问需登录接口返回 `CodeNotLoggedIn`
- ✅ **TcpConnection/TcpServer**：集成SessionManager，断连清除会话
- ✅ **协议对齐**：与成员2的字段完全对齐
  - 登录返回：userId/phone/nickname/avatar/balance/status
  - 充值返回：balance（新余额）
  - 头像方案：data-URL格式（`data:image/*;base64,...`）

**下一步**：Commit 4 - 充电站与充电桩查询服务（station.nearby / station.detail）
